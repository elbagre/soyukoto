import React from 'react';
import { Link } from 'react-router';

const App = ({ children }) => (
  <div>
    <header>
      <h1>WOAH DUDE WOW</h1>
    </header>
    {children}
  </div>
);

export default App;
